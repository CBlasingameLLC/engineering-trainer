Personal-only content.

Packs derived from textbooks or other third-party sources live here with
`licenseTier: "personal-only"`. This directory is gitignored and excluded
from release builds, so owned material can sharpen your own training without
entering a shippable artifact.

Import with `pnpm content import --personal`.
